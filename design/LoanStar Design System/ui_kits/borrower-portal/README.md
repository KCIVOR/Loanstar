# Borrower Portal

Borrower-facing dashboard: outstanding balance hero (gradient), payment-due alert with a working "Pay Now" confirmation modal, quick actions, 6-stage application-progress tracker, and an upcoming-payments schedule preview.

Source: `Borrower Portal.dc.html` in the original mockups.

Files: `BorrowerPortalApp.jsx` (`BorrowerPortalApp`), `index.html`.
